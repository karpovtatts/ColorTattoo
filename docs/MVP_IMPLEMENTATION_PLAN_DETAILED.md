# Детальный план реализации MVP

## Статус проекта

✅ **Этап 1 завершен** (см. `STAGE_1_COMPLETED.md`)
- Базовая инфраструктура: React + TypeScript + Vite
- Структура проекта и роутинг настроены
- Базовые UI компоненты созданы
- Типы данных определены

---

## Обзор MVP

**Цель:** Веб-приложение для тату-мастеров, помогающее подобрать рецепт смешивания цветов для достижения целевого оттенка.

**Ключевые функции MVP:**
1. Ввод целевого цвета (текст/код, цветовой круг)
2. Настройка пользовательской палитры
3. Подбор рецепта смешивания
4. Анализ чистый/грязный (R01, R02)
5. Анализ тёплый/холодный
6. Показ ближайшего цвета, если точный недостижим
7. Сохранение рецептов и заметок

**Критерии успеха:**
- Тату-мастер реально использует приложение перед сеансом
- Исчезает ступор «как это мешать»
- Приложение помогает избежать грязных оттенков в коже

---

## Этап 2: Модель цвета и цветовые утилиты

### 2.1 Цветовые пространства и конвертация

**Задачи:**
- [ ] Создать модуль `src/utils/colorConversions.ts`
- [ ] Реализовать конвертацию RGB → HSL
- [ ] Реализовать конвертацию HSL → RGB
- [ ] Реализовать конвертацию HEX → RGB
- [ ] Реализовать конвертацию RGB → HEX
- [ ] Реализовать валидацию цветовых значений (проверка диапазонов)
- [ ] Добавить функции нормализации (приведение к стандартному формату)

**Детали реализации:**
- RGB: значения 0-255
- HSL: H 0-360, S 0-100, L 0-100
- HEX: формат #RRGGBB (uppercase или lowercase, с/без #)
- Обработка граничных случаев (NaN, Infinity, вне диапазона)

**Файлы:**
- `src/utils/colorConversions.ts` (новый)
- `src/utils/index.ts` (экспорт функций)

**Тесты (концептуальные):**
- Конвертация туда-обратно должна давать исходный цвет (с допустимой погрешностью)
- Граничные значения (0, 255, 360, 100)

---

### 2.2 Базовые цветовые операции

**Задачи:**
- [ ] Создать модуль `src/utils/colorOperations.ts`
- [ ] Реализовать функцию смешивания цветов (аддитивное смешивание RGB)
- [ ] Реализовать расчет результирующего цвета из пропорций ингредиентов
- [ ] Реализовать функцию расчета яркости (luminance)
- [ ] Реализовать функцию расчета насыщенности (saturation)
- [ ] Реализовать функцию расчета расстояния между цветами (Delta E или простая евклидова в RGB/HSL)
- [ ] Реализовать функцию поиска ближайшего цвета из палитры

**Детали реализации:**
- Смешивание: взвешенное среднее RGB по пропорциям
- Формула яркости: `0.299*R + 0.587*G + 0.114*B` (или использовать L из HSL)
- Расстояние: евклидово расстояние в RGB пространстве или Delta E (более точное, но сложнее)
- Ближайший цвет: минимальное расстояние до целевого

**Файлы:**
- `src/utils/colorOperations.ts` (новый)

**Алгоритм смешивания:**
```typescript
// Псевдокод
function mixColors(ingredients: RecipeIngredient[]): RGB {
  let totalR = 0, totalG = 0, totalB = 0, totalWeight = 0;
  for (ingredient of ingredients) {
    const color = getColorById(ingredient.colorId);
    const weight = ingredient.proportion;
    totalR += color.rgb.r * weight;
    totalG += color.rgb.g * weight;
    totalB += color.rgb.b * weight;
    totalWeight += weight;
  }
  return {
    r: Math.round(totalR / totalWeight),
    g: Math.round(totalG / totalWeight),
    b: Math.round(totalB / totalWeight)
  };
}
```

---

### 2.3 Расширение типов данных

**Задачи:**
- [ ] Дополнить `src/types/index.ts` недостающими типами
- [ ] Добавить тип для предупреждений (Warning)
- [ ] Добавить тип для объяснений (Explanation)
- [ ] Добавить тип для результата подбора рецепта (RecipeResult)
- [ ] Добавить тип для состояния недостижимого цвета (UnreachableColorResult)

**Новые типы:**
```typescript
export interface Warning {
  type: 'dirty' | 'black_usage' | 'unreachable' | 'other'
  message: string
  severity: 'low' | 'medium' | 'high'
}

export interface RecipeResult {
  recipe: Recipe
  analysis: ColorAnalysis
  warnings: Warning[]
  isExactMatch: boolean
  distance?: number // расстояние до целевого цвета
}

export interface UnreachableColorResult {
  targetColor: Color
  nearestColor: Color
  distance: number
  explanation: string
}
```

**Файлы:**
- `src/types/index.ts` (обновить)

---

## Этап 3: Ввод целевого цвета (F01 - частично)

### 3.1 Ввод через текст/код

**Задачи:**
- [ ] Обновить `src/pages/HomePage.tsx` с формой ввода
- [ ] Добавить поле ввода HEX (#RRGGBB)
- [ ] Добавить поля ввода RGB (R, G, B отдельно или одной строкой)
- [ ] Реализовать парсинг введенных значений
- [ ] Реализовать валидацию ввода (показ ошибок)
- [ ] Добавить предпросмотр цвета (цветной квадрат)
- [ ] Синхронизация между HEX и RGB полями

**UI компоненты:**
- Использовать существующий `Input` компонент
- Создать компонент `ColorPreview` для отображения цвета
- Создать компонент `ColorInput` (обертка над Input с валидацией)

**Файлы:**
- `src/pages/HomePage.tsx` (обновить)
- `src/components/ColorInput/ColorInput.tsx` (новый)
- `src/components/ColorInput/ColorInput.css` (новый)
- `src/components/ColorPreview/ColorPreview.tsx` (новый)
- `src/components/ColorPreview/ColorPreview.css` (новый)

**Логика:**
- При вводе HEX → автоматически обновлять RGB поля
- При вводе RGB → автоматически обновлять HEX поле
- Валидация в реальном времени или при потере фокуса
- Сохранение целевого цвета в состоянии (useState или контекст)

---

### 3.2 Цветовой круг (Color Picker)

**Задачи:**
- [ ] Исследовать библиотеки цветовых пикеров для React (react-color, @uiw/react-color-picker) или создать свой
- [ ] Интегрировать/создать компонент цветового круга
- [ ] Реализовать выбор цвета через HSL (Hue, Saturation, Lightness)
- [ ] Синхронизация с текстовыми полями (HEX/RGB)
- [ ] Отображение выбранного цвета

**Варианты:**
- Использовать готовую библиотеку (быстрее, но зависимость)
- Создать свой компонент (больше контроля, но больше работы)

**Рекомендация:** Начать с библиотеки `react-color` для скорости, при необходимости заменить на свой компонент.

**Файлы:**
- `src/components/ColorPicker/ColorPicker.tsx` (новый)
- `src/components/ColorPicker/ColorPicker.css` (новый)
- `src/pages/HomePage.tsx` (интеграция)

**Зависимости:**
- Добавить `react-color` в `package.json` (если используем библиотеку)

---

### 3.3 Обработка и сохранение целевого цвета

**Задачи:**
- [ ] Создать контекст или сервис для управления состоянием целевого цвета
- [ ] Реализовать нормализацию введенного цвета (приведение к единому формату Color)
- [ ] Сохранение целевого цвета в состоянии приложения
- [ ] Передача целевого цвета на страницу рецепта (через роутинг или состояние)

**Варианты управления состоянием:**
- React Context API (для глобального состояния)
- React Router state (для передачи между страницами)
- LocalStorage (для сохранения между сессиями)

**Рекомендация:** Использовать React Context для целевого цвета и палитры.

**Файлы:**
- `src/contexts/ColorContext.tsx` (новый) - или
- `src/services/colorState.ts` (новый)

---

## Этап 4: Настройка палитры пользователя (F02)

### 4.1 Интерфейс управления палитрой

**Задачи:**
- [ ] Обновить `src/pages/PalettePage.tsx`
- [ ] Реализовать отображение текущей палитры (сетка цветных квадратов)
- [ ] Добавить кнопку "Добавить цвет"
- [ ] Реализовать модальное окно/форму для добавления цвета
- [ ] Добавить возможность редактирования существующего цвета
- [ ] Добавить возможность удаления цвета из палитры
- [ ] Добавить поле для названия цвета (опционально)
- [ ] Валидация: минимум 2 цвета в палитре, уникальность цветов

**UI компоненты:**
- Компонент `ColorSwatch` для отображения цвета в палитре
- Компонент `AddColorModal` или форма на странице
- Использовать `ColorInput` и `ColorPicker` из Этапа 3

**Файлы:**
- `src/pages/PalettePage.tsx` (обновить)
- `src/components/ColorSwatch/ColorSwatch.tsx` (новый)
- `src/components/ColorSwatch/ColorSwatch.css` (новый)
- `src/components/AddColorModal/AddColorModal.tsx` (новый, опционально)

**Визуализация:**
- Сетка цветных квадратов с названиями (если есть)
- При наведении/клике - показать HEX код
- Кнопка удаления на каждом цвете (иконка корзины)

---

### 4.2 Хранение палитры

**Задачи:**
- [ ] Реализовать сохранение палитры в localStorage
- [ ] Реализовать загрузку палитры при старте приложения
- [ ] Добавить предустановленную базовую палитру (если палитра пуста)
- [ ] Синхронизация палитры между компонентами (через Context)

**Структура данных в localStorage:**
```typescript
{
  userPalette: {
    colors: Color[]
  }
}
```

**Предустановленная палитра:**
- Базовые цвета: красный, синий, желтый, белый, черный
- Или пустая палитра с подсказкой добавить цвета

**Файлы:**
- `src/services/paletteStorage.ts` (новый)
- `src/contexts/PaletteContext.tsx` (новый) - или объединить с ColorContext

**Функции:**
- `savePalette(palette: UserPalette): void`
- `loadPalette(): UserPalette | null`
- `getDefaultPalette(): UserPalette`

---

### 4.3 Валидация палитры

**Задачи:**
- [ ] Реализовать проверку минимального количества цветов (минимум 2)
- [ ] Реализовать проверку уникальности цветов (по RGB или HEX)
- [ ] Показывать предупреждения при невалидной палитре
- [ ] Блокировать подбор рецепта, если палитра невалидна

**Логика:**
- Два цвета считаются одинаковыми, если расстояние между ними < порога (например, 5 единиц в RGB)
- Или точное совпадение HEX

**Файлы:**
- `src/utils/paletteValidation.ts` (новый)

---

## Этап 5: Алгоритм подбора рецепта (F01 - основная часть)

### 5.1 Базовый алгоритм подбора

**Задачи:**
- [ ] Создать модуль `src/services/recipeFinder.ts`
- [ ] Реализовать алгоритм поиска комбинации цветов
- [ ] Ограничение на количество ингредиентов (максимум 3-4 цвета)
- [ ] Расчет пропорций для найденной комбинации
- [ ] Оптимизация производительности (кэширование, ранний выход)

**Алгоритм (вариант 1 - перебор):**
1. Перебрать все комбинации из 1, 2, 3, 4 цветов из палитры
2. Для каждой комбинации попробовать разные пропорции (дискретные значения)
3. Вычислить результирующий цвет
4. Найти комбинацию с минимальным расстоянием до целевого цвета

**Алгоритм (вариант 2 - оптимизация):**
- Использовать градиентный спуск или генетический алгоритм
- Более эффективно для больших палитр

**Рекомендация для MVP:** Начать с перебора для 1-3 цветов, оптимизировать позже.

**Файлы:**
- `src/services/recipeFinder.ts` (новый)

**Функции:**
```typescript
function findRecipe(
  targetColor: Color,
  palette: UserPalette,
  maxIngredients: number = 3
): RecipeResult | UnreachableColorResult
```

**Параметры поиска:**
- Максимум ингредиентов: 3 (можно настроить)
- Шаг пропорций: 0.1 (10%) или дискретные значения (1:1, 1:2, 2:1, и т.д.)
- Порог точности: расстояние < 5 единиц = точное совпадение

---

### 5.2 Обработка недостижимых цветов

**Задачи:**
- [ ] Определить критерий "недостижимый цвет" (расстояние > порога)
- [ ] Реализовать поиск ближайшего достижимого цвета
- [ ] Реализовать расчет и объяснение, почему цвет недостижим
- [ ] Генерация понятного объяснения для пользователя

**Критерии:**
- Цвет недостижим, если минимальное расстояние > порога (например, 30 единиц)
- Или если целевой цвет вне гаммы палитры (например, слишком насыщенный)

**Объяснения:**
- "Цвет недостижим, так как слишком насыщенный для вашей палитры"
- "Цвет недостижим, так как требует оттенка, которого нет в палитре"
- Показать ближайший достижимый цвет и разницу

**Файлы:**
- `src/services/recipeFinder.ts` (дополнить)

---

### 5.3 Генерация рецепта

**Задачи:**
- [ ] Форматирование рецепта в читаемый вид
- [ ] Поддержка разных форматов пропорций (части, проценты, соотношения)
- [ ] Создание объекта Recipe с метаданными
- [ ] Генерация ID для рецепта

**Форматы пропорций:**
- Части: "2 части красного, 1 часть синего"
- Проценты: "66% красного, 33% синего"
- Соотношение: "2:1"

**Файлы:**
- `src/utils/recipeFormatter.ts` (новый)

---

## Этап 6: Анализ чистый/грязный (F03, R01, R02)

### 6.1 Реализация правил определения грязного цвета

**Задачи:**
- [ ] Создать модуль `src/services/colorAnalysis.ts`
- [ ] Реализовать правило R01: определение грязного цвета
- [ ] Реализовать правило R02: обнаружение использования черного
- [ ] Реализовать эвристики для определения "грязности"

**Правило R01 (чистый vs грязный):**
- Цвет считается грязным, если:
  - Низкая насыщенность (S < 20-30) И средняя/низкая яркость
  - Уход в серо-коричневый диапазон
  - Потеря хроматики

**Правило R02 (использование черного):**
- Обнаружить использование черного цвета в рецепте для цветных оттенков
- Черный допустим только в ЧБ работах и техниках типа silvera
- Для цветных работ: черный не затемняет, а загрязняет

**Эвристики:**
- Проверка насыщенности результирующего цвета
- Проверка наличия черного в ингредиентах (для цветных целевых цветов)
- Анализ цветового тона (не уходит ли в серый/коричневый)

**Файлы:**
- `src/services/colorAnalysis.ts` (новый)
- `src/utils/dirtyColorDetection.ts` (новый, опционально)

**Функции:**
```typescript
function analyzeColorCleanliness(
  color: Color,
  recipe: Recipe
): {
  isClean: boolean
  isDirty: boolean
  warnings: Warning[]
  explanations: string[]
}
```

---

### 6.2 Анализ рецепта

**Задачи:**
- [ ] Проверка рецепта на наличие проблемных комбинаций
- [ ] Определение риска "грязного" результата
- [ ] Расчет вероятности потери хроматики в коже
- [ ] Генерация предупреждений

**Проверки:**
- Наличие черного в ингредиентах для цветного целевого цвета → предупреждение
- Низкая насыщенность результирующего цвета → предупреждение
- Смешивание комплементарных цветов в равных пропорциях → риск серого

**Файлы:**
- `src/services/colorAnalysis.ts` (дополнить)

---

### 6.3 Предупреждения и объяснения

**Задачи:**
- [ ] Генерация текстовых предупреждений для рискованных рецептов
- [ ] Объяснение причины проблемы
- [ ] Предложение альтернатив (если возможно)

**Типы предупреждений:**
- "⚠️ Использование черного загрязняет цветные оттенки. Рекомендуется затемнять через комплементарные цвета."
- "⚠️ Результирующий цвет может потерять хроматику в коже из-за низкой насыщенности."
- "⚠️ Смешивание комплементарных цветов может дать грязный серый оттенок."

**Альтернативы:**
- Если обнаружен черный → предложить затемнение через комплементарный цвет
- Если низкая насыщенность → предложить использовать более насыщенные базовые цвета

**Файлы:**
- `src/services/colorAnalysis.ts` (дополнить)
- `src/utils/warningMessages.ts` (новый, опционально)

---

## Этап 7: Анализ тёплый/холодный (F04)

### 7.1 Определение теплоты цвета

**Задачи:**
- [ ] Реализовать алгоритм определения теплого/холодного характера цвета
- [ ] Расчет "температуры" цвета в цветовом пространстве
- [ ] Учет влияния смешивания на температуру

**Алгоритм:**
- Теплые цвета: оттенки красного, оранжевого, желтого (H примерно 0-90, 330-360)
- Холодные цвета: оттенки синего, зеленого, фиолетового (H примерно 90-270)
- Нейтральные: серые, бежевые (низкая насыщенность)

**Расчет:**
- Основываться на Hue (оттенок) из HSL
- Учитывать насыщенность (при низкой S цвет ближе к нейтральному)

**Файлы:**
- `src/services/colorAnalysis.ts` (дополнить)
- `src/utils/warmCoolDetection.ts` (новый, опционально)

**Функции:**
```typescript
function analyzeColorTemperature(color: Color): {
  isWarm: boolean
  isCool: boolean
  isNeutral: boolean
  temperature: number // -1 (холодный) до +1 (теплый)
}
```

---

### 7.2 Анализ и объяснение

**Задачи:**
- [ ] Определение теплоты целевого цвета
- [ ] Определение теплоты результирующего цвета из рецепта
- [ ] Сравнение и объяснение характера цвета
- [ ] Генерация текстовых объяснений

**Объяснения:**
- "Цвет теплый, с преобладанием красных и оранжевых оттенков"
- "Цвет холодный, с преобладанием синих и зеленых оттенков"
- "Цвет нейтральный, близок к серому"

**Файлы:**
- `src/services/colorAnalysis.ts` (дополнить)

---

### 7.3 Визуализация

**Задачи:**
- [ ] Создать визуальные индикаторы теплоты
- [ ] Цветовая шкала или иконки (🔥/❄️)
- [ ] Сравнение целевого и результирующего цвета по температуре

**UI компоненты:**
- Компонент `TemperatureIndicator` для отображения теплоты
- Иконки или цветовая полоса

**Файлы:**
- `src/components/TemperatureIndicator/TemperatureIndicator.tsx` (новый)
- `src/components/TemperatureIndicator/TemperatureIndicator.css` (новый)

---

## Этап 8: Отображение рецепта и результатов (F01, F03, F04, F05)

### 8.1 Экран результатов

**Задачи:**
- [ ] Обновить `src/pages/RecipePage.tsx`
- [ ] Отображение целевого цвета
- [ ] Отображение результирующего цвета (из рецепта)
- [ ] Визуальное сравнение (рядом или наложением)
- [ ] Отображение рецепта (ингредиенты + пропорции)
- [ ] Отображение расстояния/разницы между цветами

**UI компоненты:**
- Использовать `ColorPreview` для отображения цветов
- Компонент `RecipeDisplay` для отображения рецепта
- Компонент `ColorComparison` для сравнения цветов

**Файлы:**
- `src/pages/RecipePage.tsx` (обновить)
- `src/components/RecipeDisplay/RecipeDisplay.tsx` (новый)
- `src/components/RecipeDisplay/RecipeDisplay.css` (новый)
- `src/components/ColorComparison/ColorComparison.tsx` (новый)
- `src/components/ColorComparison/ColorComparison.css` (новый)

**Макет:**
```
[Целевой цвет] [Результирующий цвет] [Разница]
[Рецепт: ингредиенты и пропорции]
[Анализ: чистый/грязный, тёплый/холодный]
[Предупреждения]
```

---

### 8.2 Визуализация информации

**Задачи:**
- [ ] Индикаторы чистый/грязный (цветовые метки, иконки)
- [ ] Индикаторы тёплый/холодный (использовать TemperatureIndicator)
- [ ] Предупреждения (выделение, цвет, иконки)
- [ ] Объяснения и подсказки (текстовые блоки)

**UI компоненты:**
- Компонент `WarningBadge` для предупреждений
- Компонент `AnalysisSection` для анализа цвета
- Использовать существующие компоненты Button для действий

**Файлы:**
- `src/components/WarningBadge/WarningBadge.tsx` (новый)
- `src/components/WarningBadge/WarningBadge.css` (новый)
- `src/components/AnalysisSection/AnalysisSection.tsx` (новый)
- `src/components/AnalysisSection/AnalysisSection.css` (новый)

**Стили предупреждений:**
- Высокий риск: красный фон/рамка
- Средний риск: оранжевый/желтый
- Низкий риск: синий/информационный

---

### 8.3 Обработка недостижимых цветов (F05)

**Задачи:**
- [ ] Отображение ближайшего достижимого цвета
- [ ] Визуальное сравнение целевого и ближайшего
- [ ] Объяснение, почему точный цвет недостижим
- [ ] Показ разницы (визуально и численно)
- [ ] Кнопка "Использовать ближайший цвет"

**UI:**
- Четкое указание, что это не точное совпадение
- Визуальное выделение разницы
- Текстовое объяснение причины

**Файлы:**
- `src/pages/RecipePage.tsx` (дополнить)
- `src/components/UnreachableColorMessage/UnreachableColorMessage.tsx` (новый)

---

## Этап 9: Сохранение рецептов и заметок (F06)

### 9.1 Интерфейс сохранения

**Задачи:**
- [ ] Добавить кнопку "Сохранить рецепт" на странице результатов
- [ ] Создать форму/модальное окно для добавления заметок
- [ ] Редактирование сохраненных заметок
- [ ] Валидация (минимум название или автоматическое название)

**UI компоненты:**
- Модальное окно `SaveRecipeModal`
- Форма с полем для заметок (textarea)
- Автогенерация названия: "Рецепт для #RRGGBB" или дата

**Файлы:**
- `src/components/SaveRecipeModal/SaveRecipeModal.tsx` (новый)
- `src/components/SaveRecipeModal/SaveRecipeModal.css` (новый)
- `src/pages/RecipePage.tsx` (интеграция)

---

### 9.2 Хранение данных

**Задачи:**
- [ ] Расширить структуру данных Recipe (добавить название, заметки)
- [ ] Реализовать сохранение в localStorage
- [ ] Генерация уникальных ID для рецептов
- [ ] Timestamp создания/изменения
- [ ] Функции CRUD для рецептов

**Структура данных:**
```typescript
export interface SavedRecipe extends Recipe {
  name?: string
  notes?: string
  tags?: string[] // опционально для будущего
}
```

**Файлы:**
- `src/services/recipeStorage.ts` (новый)
- `src/types/index.ts` (обновить)

**Функции:**
- `saveRecipe(recipe: SavedRecipe): void`
- `loadRecipes(): SavedRecipe[]`
- `deleteRecipe(id: string): void`
- `updateRecipe(id: string, updates: Partial<SavedRecipe>): void`

---

### 9.3 Просмотр сохраненных рецептов

**Задачи:**
- [ ] Обновить `src/pages/SavedRecipesPage.tsx`
- [ ] Отображение списка сохраненных рецептов
- [ ] Визуализация (миниатюры цветов, названия, даты)
- [ ] Открытие сохраненного рецепта (переход на RecipePage)
- [ ] Удаление сохраненных рецептов
- [ ] Поиск/фильтрация (опционально для MVP)

**UI компоненты:**
- Компонент `RecipeCard` для отображения рецепта в списке
- Сетка или список карточек
- Кнопка удаления на каждой карточке

**Файлы:**
- `src/pages/SavedRecipesPage.tsx` (обновить)
- `src/components/RecipeCard/RecipeCard.tsx` (новый)
- `src/components/RecipeCard/RecipeCard.css` (новый)

**Макет карточки:**
```
[Миниатюра целевого цвета] [Название/Дата]
[Краткая информация: ингредиенты]
[Кнопки: Открыть | Удалить]
```

---

## Этап 10: Интеграция и полировка

### 10.1 Связывание всех компонентов

**Задачи:**
- [ ] Проверить передачу данных между страницами
- [ ] Настроить Context API для глобального состояния (целевой цвет, палитра)
- [ ] Обработка ошибок на всех этапах
- [ ] Валидация данных перед передачей между компонентами

**Глобальное состояние:**
- `ColorContext`: целевой цвет, результат подбора
- `PaletteContext`: пользовательская палитра
- Или объединить в один `AppContext`

**Файлы:**
- `src/contexts/AppContext.tsx` (новый, если объединяем)
- Обновить все страницы для использования контекста

---

### 10.2 UX улучшения

**Задачи:**
- [ ] Добавить загрузочные состояния (spinner при подборе рецепта)
- [ ] Сообщения об ошибках (невалидный ввод, пустая палитра)
- [ ] Подтверждения действий (удаление рецепта, очистка палитры)
- [ ] Подсказки и туториалы (опционально, можно отложить)
- [ ] Обратная связь при сохранении (toast notification)

**UI компоненты:**
- Компонент `LoadingSpinner`
- Компонент `ErrorMessage`
- Компонент `ConfirmDialog` (опционально)
- Toast notifications (можно использовать библиотеку или создать простой)

**Файлы:**
- `src/components/LoadingSpinner/LoadingSpinner.tsx` (новый)
- `src/components/ErrorMessage/ErrorMessage.tsx` (новый)

---

### 10.3 Адаптивность

**Задачи:**
- [ ] Проверить адаптивный дизайн на мобильных устройствах
- [ ] Оптимизация для touch-интерфейсов (размеры кнопок, отступы)
- [ ] Тестирование на разных размерах экранов
- [ ] Улучшение мобильной навигации

**Проверка:**
- Минимальная ширина: 320px (мобильные)
- Максимальная ширина: 1920px (десктоп)
- Touch targets: минимум 44x44px

---

### 10.4 Производительность

**Задачи:**
- [ ] Оптимизация алгоритма подбора (если медленный)
- [ ] Кэширование результатов подбора для одинаковых входных данных
- [ ] Ленивая загрузка компонентов (React.lazy, если нужно)
- [ ] Оптимизация рендеринга больших списков (виртуализация, если нужно)

**Оптимизации:**
- Мемоизация вычислений (useMemo)
- Дебаунсинг ввода цвета
- Кэш результатов в памяти или localStorage

---

## Этап 11: Тестирование и финализация

### 11.1 Функциональное тестирование

**Задачи:**
- [ ] Тестирование всех основных flow:
  - Ввод цвета → подбор рецепта → сохранение
  - Настройка палитры → подбор рецепта
  - Просмотр сохраненных рецептов
- [ ] Тестирование граничных случаев:
  - Пустая палитра
  - Невалидный ввод цвета
  - Один цвет в палитре
  - Очень большая палитра (10+ цветов)
- [ ] Тестирование правил R01, R02 на известных примерах
- [ ] Тестирование алгоритма подбора на различных цветах

**Тестовые сценарии:**
1. Целевой цвет = цвет из палитры → должен найти точное совпадение
2. Целевой цвет вне гаммы → должен показать ближайший
3. Рецепт с черным для цветного → должно быть предупреждение
4. Низкая насыщенность → должно быть предупреждение о грязности

---

### 11.2 Тестирование правил

**Задачи:**
- [ ] Проверка правил R01 на тестовых случаях:
  - Грязный цвет (низкая насыщенность, серо-коричневый)
  - Чистый цвет (высокая насыщенность)
- [ ] Проверка правил R02:
  - Черный в рецепте для цветного → предупреждение
  - Черный в рецепте для ЧБ → без предупреждения
- [ ] Проверка анализа теплоты:
  - Теплые цвета (красный, оранжевый)
  - Холодные цвета (синий, зеленый)
  - Нейтральные (серый)

---

### 11.3 Исправление багов

**Задачи:**
- [ ] Исправление найденных проблем
- [ ] Оптимизация производительности
- [ ] Улучшение точности алгоритмов
- [ ] Улучшение формулировок предупреждений и объяснений

---

### 11.4 Финальная полировка

**Задачи:**
- [ ] Проверка соответствия спецификациям (все документы из /docs/spec)
- [ ] Проверка UI/UX согласно `05_ui_ux.md`
- [ ] Финальная проверка критериев успеха (`success_criteria.md`)
- [ ] Проверка всех правил из /docs/rules
- [ ] Документация для пользователя (опционально)

**Критерии готовности:**
- ✅ Все функции MVP реализованы
- ✅ Правила R01, R02 работают корректно
- ✅ Приложение быстрое и отзывчивое
- ✅ UI понятный и не отвлекающий
- ✅ Предупреждения точные и полезные

---

## Зависимости между этапами

```
Этап 1 (Инфраструктура) ✅ ЗАВЕРШЕН
    ↓
Этап 2 (Модель цвета) ← НАЧАТЬ ЗДЕСЬ
    ↓
Этап 3 (Ввод цвета) ←→ Этап 4 (Палитра)
    ↓
Этап 5 (Алгоритм подбора)
    ↓
Этап 6 (Анализ грязный) + Этап 7 (Анализ тёплый)
    ↓
Этап 8 (Отображение результатов)
    ↓
Этап 9 (Сохранение)
    ↓
Этап 10 (Интеграция)
    ↓
Этап 11 (Тестирование)
```

---

## Приоритеты реализации

### MVP Core (обязательно):
- ✅ Этап 1: Инфраструктура
- ⏳ Этапы 2-9: Полный функционал "ввёл цвет → получил рецепт"
- ⏳ Этапы 10-11: Интеграция и тестирование

### Отложено на потом:
- F07: Библиотека цветовых референсов
- F08: Фото + пипетка (низкий приоритет, упомянуто в спецификации)
- F09: Мини-игра для тренировки
- R03, R04: Дополнительные правила (если не описаны в текущих документах)

---

## Открытые вопросы для уточнения

1. **Алгоритм подбора рецепта:** Какой подход использовать? (перебор для MVP, оптимизация позже)
2. **Правила R03, R04:** Нужно ли их реализовывать в MVP? (не найдены в документах)
3. **Формат пропорций:** Какие форматы предпочтительны? (части, проценты, соотношения - реализовать все)
4. **Серверное хранение:** Нужен ли backend в MVP? (согласно спецификации - нет, только localStorage)
5. **Предустановленные палитры:** Нужны ли базовые палитры по умолчанию? (рекомендуется добавить)

---

## Критерии готовности MVP

Согласно `success_criteria.md`, MVP считается успешным, если:
- ✅ Тату-мастер реально использует его перед сеансом
- ✅ Исчезает ступор «как это мешать»
- ✅ Приложение помогает избежать грязных оттенков в коже

Это означает, что MVP должен быть:
- Быстрым в использовании (< 30 секунд от ввода до рецепта)
- Точным в подборе рецептов (минимальное расстояние до целевого)
- Надежным в предупреждениях о грязных смесях (правила R01, R02)
- Понятным в объяснениях (четкие формулировки)

---

## Следующие шаги

**Немедленно начать:**
1. Этап 2.1: Цветовые пространства и конвертация
2. Этап 2.2: Базовые цветовые операции
3. Этап 2.3: Расширение типов данных

**После завершения Этапа 2:**
- Перейти к Этапу 3 (Ввод цвета) и Этапу 4 (Палитра) параллельно или последовательно

---

*План создан на основе всех документов из /docs*
*Дата создания: 2024*
*Версия: 1.0*

